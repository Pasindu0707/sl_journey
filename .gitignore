# Node / Next.js (the nextJS/ app)
node_modules/
nextJS/node_modules/
nextJS/.next/
nextJS/out/
nextJS/next-env.d.ts

# Build / OS / editor noise
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local env files
.env*
!.env.example

# Local design sources / working materials — logo originals, their zips, and the
# old content-capture doc. The site ships the exported logo variants in
# nextJS/public/assets/logo/, so these raw originals stay out of the repo.
design-sources/
